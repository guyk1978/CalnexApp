/**
 * Deprecated: Google/Bing sitemap ping endpoints were removed.
 * Sitemap is written by `npm run generate-sitemap` during build.
 */
console.log(
  "[sitemap:ping] skipped — sitemap is generated at build time (search-engine ping APIs deprecated)."
);
